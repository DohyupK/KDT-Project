# 2026-07-29 업무일지

## 요약

1. 실측 Outcome 파이프라인 완결 (`outcome_residual_li` · Undo 가드 · EDA 검증).
2. **보안 전체화면 오버레이** + **보안 전용 RAG** (Qdrant/BM25/RRF/bge-m3·reranker CPU).

## 변경 (Outcome)

### Backend

- [`backend/src/services/outcomeBounds.ts`](../../backend/src/services/outcomeBounds.ts) — 검증 상수
  - capacity **130.00–250.00**, residual_li **500.00–8000.00**, 소수 2자리
- [`controlStore.ts`](../../backend/src/services/controlStore.ts) — `outcome_residual_li` 컬럼(ALTER), UPDATE 3필드, reverted → throw
- [`control.ts`](../../backend/src/routes/control.ts) — payload/응답에 `outcome_residual_li`

### Frontend

- [`frontend/src/lib/outcomeBounds.ts`](../../frontend/src/lib/outcomeBounds.ts) — BE와 동일 상수
- [`GlobalChatbot.tsx`](../../frontend/src/components/chat/GlobalChatbot.tsx) — 실측 폼 · Undo 게이트
- [`aiApi.ts`](../../frontend/src/api/aiApi.ts) — outcome_residual_li 타입·POST

## 변경 (보안 전체화면 · RAG)

### Frontend

- [`GlobalChatbot.tsx`](../../frontend/src/components/chat/GlobalChatbot.tsx) — 좌측 Maximize → `z-[70]` 오버레이에 `SecurityChatbot` (Esc/Minimize 닫기)
- [`SecurityChatbot.tsx`](../../frontend/src/components/chat/SecurityChatbot.tsx) — `variant=embedded|page` · `[출처:]` 클릭 · 청크 사이드 패널
- [`securityChatApi.ts`](../../frontend/src/api/securityChatApi.ts) — `sources[]`

### Backend

- [`securityChat.ts`](../../backend/src/routes/securityChat.ts) — `sources` 패스스루

### ai-service

- [`agent/rag_engine.py`](../../ai-service/agent/rag_engine.py) — Self-Query 필터 · Qdrant+BM25 · RRF · CPU rerank
- [`agent/secure_graph.py`](../../ai-service/agent/secure_graph.py) — LangGraph retrieve/gate/generate
- [`ingest_secure.py`](../../ai-service/ingest_secure.py) · fixture `data/secure_docs/*.md`
- [`docs/references/secure-rag.md`](../references/secure-rag.md)

## Outcome 스모크 (PASS)

| 케이스 | HTTP | 결과 |
|--------|------|------|
| approve → outcome 정상 (cap=200.5, residual=2800.25) | 200 | `ok`, 파싱값 echo |
| capacity=20 (범위 밖) | 400 | `outcome_capacity must be between 130 and 250` |
| residual=2800.255 (소수 3자리) | 400 | `…allows at most 2 decimal place(s)` |
| residual=100 (범위 밖) | 400 | `outcome_residual_li must be between 500 and 8000` |
| revert 후 outcome | 400 | `reverted events cannot record outcome` |

## Secure RAG 스모크

- Qdrant Docker `kdt-qdrant` :6333 · `ingest_secure.py` → collection `secure_docs` **2 points**
- retrieve: `소성 온도 SOP` → title hit score≈0.997 · `점심 메뉴` → rerank 0.0 → min_score 필터로 no-docs 경로
- ai-service 재기동 후 브라우저: Maximize 전체화면 · 출처 클릭 패널 확인

## 다음

- RAG 스모크 상시화 · TS 불량률 · Login/MariaDB 홀딩
