# 2026-08-15

- **ingest 상시 아님 (SSOT):** `python ingest_secure.py` 는 Qdrant `secure_docs` 를 한 번 채우는 작업. 표·기동 순서만 [`documents-watcher-qdrant.md`](../references/documents-watcher-qdrant.md) §6. 다른 문서는 그 절로만 연결 (표 복붙 없음). Lightsail 터널·Docker·secure-rag·vllm-setup·보안 챗 가이드·packages·feature-catalog·LLM 튜닝.
- **보안 챗 실패 문구 분리:** 컬렉션 없으면 `RAG_NOT_READY_REPLY` (vLLM 미호출). `:8001` 실패는 `OFFLINE_REPLY`. 표: [`security-chatbot-guide.md`](../references/security-chatbot-guide.md).
- **오버레이 URL:** 「보안 상담」은 `history.pushState('/security')` (`router.push` 아님). `/main` 유지. `PageChatRouteReset`은 `/security` 출입 시 스킵. 같은 가이드.
