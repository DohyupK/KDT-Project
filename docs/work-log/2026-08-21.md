# 2026-08-21 작업 기록

## Production build 복구

- `analysisLotSyncPoller.ts`의 `fillRecommendedActionsForLots` 누락 import를 추가했다.
- `/issue`와 `/inquiry`의 `useSearchParams()` 사용 컴포넌트를 `Suspense` 경계 아래로 이동했다.
- 백엔드 `npm.cmd run build`: 통과
- 프런트 `npm.cmd run build`: 통과, 정적 페이지 12/12 생성
- 대상 파일과 `docs/catalog.md` 원본은 `build-fix-backups/20260821-before-nonchat-build-fix/`에 보관했다.

## 생성형 LLM 준비 상태

- 일반 챗봇은 `CHAT_USE_LLM=1`과 암호화 키 저장소에 등록한 실제 API 키가 필요하다.
- 인증된 키 목록 API 확인 결과 등록 키는 0개였고 키 원문은 조회하지 않았다.
- 보안 챗봇은 클라우드 폴백 없이 로컬 `:8001/v1` LLM 서버와 PC 보안 워커가 필요하다.
- 현재 로컬 캐시에서 확인된 `BAAI/bge-m3`는 임베딩 모델이며 생성형 LLM이 아니다.

상세 전·후 코드, SHA-256, build 출력, 롤백 위치는 [`build-fix-and-llm-readiness-2026-08-21.md`](../references/build-fix-and-llm-readiness-2026-08-21.md)에 기록했다.
