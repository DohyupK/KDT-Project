# 2026-08-14

- Lightsail **`my-server-16gb`** (4 vCPU · 16GB · 320GB) 생성에 맞춰, 확정 설계를 코드/문서에 고정: 앱·DB·n8n·Qdrant는 CPU 서버, **vLLM은 이 PC GPU** (`ssh -R` → `127.0.0.1:8001`).
- Grafana iframe 호스트를 페이지에서 제거하고 루트 `.env` `NEXT_PUBLIC_GRAFANA_HOST` / `PORT` 로 모음 (`frontend/src/lib/grafanaEmbed.ts`).
- `docker-compose.yml` (Qdrant·n8n, `127.0.0.1` 바인드), `deploy/nginx-kdt.conf`, `scripts/vllm-tunnel.ps1`.
- 가이드: [`docs/guides/aws-lightsail-gpu-tunnel.md`](../guides/aws-lightsail-gpu-tunnel.md). 이 PC `DB_HOST`는 구 서버 IP를 유지 — 16GB에 덤프를 넣은 뒤에만 새 IP로 바꿀 것.
- 이슈 보고서 수동 재발송: `npm run send:one-issue-report` → n8n `send=O` (LOT `LOT-20260812-11755`)
- Lightsail Docker(n8n·Qdrant) 절차 정리: [`docs/guides/aws-lightsail-docker.md`](../guides/aws-lightsail-docker.md)  
  n8n만 AWS·backend는 PC면 콜백 불가. 포트 숫자는 루트 `.env`.
- 확정 계획: AWS는 CPU 앱 서버(16GB 권장), vLLM은 PC + SSH `-R` — [`docs/plans/2026-08-14-aws-app-pc-vllm.md`](../plans/2026-08-14-aws-app-pc-vllm.md)
- Docker 정리: 여분 n8n 3개(`goofy_sutherland` 등)·Created qdrant·`my-next-app`/`next2`/`next3` 및 해당 이미지 삭제. `docker-compose.yml` Git 충돌 해소(n8n `2.34.5` + Windows env + `127.0.0.1` 바인드). 남는 컨테이너는 `kdt-n8n` · `kdt-qdrant`만.
