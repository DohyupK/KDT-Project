# 2026-07-31 업무보고

## 한 줄 요약

보안 RAG·멀티턴·로컬 LLM 안정화와 Documents 듀얼 엔진(문서/표 분리)까지 반영했다. 인덱스에는 문서가 여러 개 있으나, 후속 질문에서 검색 0건이면 **직전 턴 문서 1건(prior_sources)** 만 재사용해 UI상 “문서 1개만 읽는” 현상이 난다.

---

## 완료 업무

### 1. 보안 문서·ingest
- 문서 루트 `Documents/` (PDF/TXT/MD ingest)
- `ingest_secure.py` + Qdrant `secure_docs` · BM25 노드 캐시

### 2. 멀티턴 (MariaDB B안)
- `user_chat_threads` / `user_chat_messages` · Prisma · Express 패스스루
- FE는 `message`+`thread_id`+`user_id`만 전송 · 서버가 history 로드

### 3. 단기·장기 기억
- 1계층: 윈도우 6 + 휴리스틱 truncate (LLM 요약 없음)
- 2계층: Qdrant `chat_history_collection` Top-K=3 soft-fail

### 4. 보안챗 안정화
- Next/Express timeout 상향 · generate 프롬프트 축소
- 빈 Gemma content ≠ 타임아웃 문구 분리 · 1회 재시도
- UI: 전송~응답 wall time 표시 (`mode`/`provider` 옆)

### 5. 채팅 복원
- `GET /chat/threads` · `GET /chat/threads/{id}/messages`
- 보안챗·일반챗 스레드 목록 / 클릭 복원 / 새 대화

### 6. Documents 듀얼 엔진 + Watchdog
- PDF/TXT → `Documents/ai-service/*.md` → full ingest
- CSV/XLSX → Move `ai-service/data/csv_lake/` → `*-profile.md`(스키마+샘플 5행만)
- Debounce 4s + ingest coalesce lock (FastAPI 비블로킹)
- 패키지: `pypdf`, `openpyxl`, `watchdog`, SQLAlchemy/PyMySQL

## 미해결·주의

- **후속 턴 “문서 1개만”**: 로그 `prior_sources n_sources=1 docs=sop-coating-v1` — 검색 miss 시 직전 출처 재사용이 원인 (아래 진단)
- 구버전 풀테이블 MD(`Documents/ai-service/cathode_*.md` 등)가 인덱스에 남아 청크를 많이 차지할 수 있음 → 듀얼 엔진 이후 lake+profile로 정리·재ingest 권장
- `SECURE_GENERATE=1`은 로컬 LLM 부하·빈답/타임아웃 가능 · 운영 기본은 `0` 검토

## 다음 액션 (제안)

1. 짧은 후속 질문에서도 retrieve miss 줄이기 (쿼리 확장 / prior 재사용 조건 완화 / min_score 점검)
2. 옛 full CSV→MD 산출물 정리 후 `ingest_secure` 재실행
3. Login·`user_id` 정식 연동 · TS 불량률(기상 CSV)은 data lake 경로로

---

# 상세 일지

## 1단계 — 보안 문서 경로

- 소스: 모노레포 루트 [`Documents/`](../../Documents/) (구 `ai-service/data/secure_docs/`)
- [`rag_engine.SECURE_DOCS_DIR`](../../ai-service/agent/rag_engine.py) + env `SECURE_DOCS_DIR`
- [`ingest_secure.py`](../../ai-service/ingest_secure.py): `.md` / `.txt` / `.pdf`(pypdf) · 미지원 확장자 스킵
- 의존성: `pypdf` (+ 멀티턴용 `SQLAlchemy`, `PyMySQL`) — `requirements.txt` / README 스택 반영
- 참조: [`docs/references/secure-rag.md`](../references/secure-rag.md)

## 2단계 — Prisma / MariaDB 스키마

- DDL: [`backend/schema.sql`](../../backend/schema.sql) · [`backend/src/sql/user_chat_tables.sql`](../../backend/src/sql/user_chat_tables.sql)
- Prisma: [`frontend/prisma/schema.prisma`](../../frontend/prisma/schema.prisma) (`users` 참조만, `channel` security|general, message `BigInt`)
- BigInt JSON: [`frontend/src/lib/bigintJson.ts`](../../frontend/src/lib/bigintJson.ts) ← root layout import

## 3단계 — B 방안 페이로드

- FE: `message` + `thread_id` + `user_id`만 ([`securityChatApi.ts`](../../frontend/src/api/securityChatApi.ts) · [`aiApi.ts`](../../frontend/src/api/aiApi.ts))
- Express 패스스루 유지 (:3001)
- ai-service: [`chat_history_store.py`](../../ai-service/agent/chat_history_store.py) 로드/저장 · prior sources로 후속 턴 (retrieve 0 + prior 없으면 `no_docs` 유지 · `SECURE_GENERATE=0` 유지)

## 적용 시 수동

1. MariaDB에 `user_chat_tables.sql` 적용 — **완료** (`DB/ai-service/apply_user_chat_tables.py` → `kdt_project`)
2. `pip install pypdf SQLAlchemy PyMySQL` — **완료**
3. `npm i` + `prisma generate` (frontend) — **완료**
4. `python ingest_secure.py` — **완료** (Qdrant `secure_docs` **6 points**, docs=`Documents/`)

로컬/스키마 산출물 위치: 루트 [`DB/`](../../DB/) · ai-service 전용 [`DB/ai-service/`](../../DB/ai-service/)  
- Qdrant chat history 메모: [`chat_history_qdrant.md`](../../DB/ai-service/chat_history_qdrant.md)  
(기존 `ai-service/DB/llm_keys.sqlite`는 유지)

## 1계층 단기 기억 (휴리스틱 · LLM 요약 배제)

- [`chat_history_store.py`](../../ai-service/agent/chat_history_store.py): `CHAT_HISTORY_WINDOW=6`, `ORDER BY created_at DESC` + `reverse()`, `heuristic_truncate` / `format_history_text_compact` (`format_history_text` 유지)
- [`secure_prompts.py`](../../ai-service/agent/secure_prompts.py): `is_summary_intent` · `format_compressed_extractive_reply` (~200자 bullet)
- [`secure_graph.py`](../../ai-service/agent/secure_graph.py): `SECURE_GENERATE=0` 안에서 요약 intent → compressed (LLM 미호출) · `no_docs` 유지
- 호출부: `compose_secure` / `main` chat → `format_history_text_compact`

## 2계층 장기 기억 (Qdrant 시맨틱 가지치기)

- [`chat_history_vector.py`](../../ai-service/agent/chat_history_vector.py): collection `chat_history_collection`, bge-m3 CPU, soft-fail
- Upsert: FastAPI `BackgroundTasks` (`/chat`, `/security-chat`)
- Search Top-K=3 → heuristic merge into `history_text` (LLM 요약 없음)
- 메모: [`DB/ai-service/chat_history_qdrant.md`](../../DB/ai-service/chat_history_qdrant.md)

## 보안챗 500 진단·안정화

- 원인: Next→Express **socket hang up** (`data: null`) + generate 프롬프트에 장기기억·잡담 과다
- Next `experimental.proxyTimeout=190s` · Express `requestTimeout` 200s
- generate: `history_for_generate` (단기만) · `format_rag_context_for_generate` · `SECURE_VLLM_MAX_TOKENS` 기본 256 · `gen_ms`/`prompt_chars` trace
- FE: hang-up 시 힌트 문구

## 빈 Gemma 응답 (empty content) 진단

- 증상: `SECURE_GENERATE=1` 시 UI 「로컬 LLM이 빈 답을 반환…」 + extractive (타임아웃 아님)
- curl 스모크 (`gemma` → `gemma@q2_k`): 단순 인사 → `content: "."` · RAG형 프롬프트 → `content: ""`, `finish_reason=stop`, `completion_tokens=1`
- 판정: **파싱 버그 아님 · 초소형 Q2_K 모델 한계** (앱이 답을 채워 넣을 수 없음)
- 조치: 로컬 `.env` `SECURE_GENERATE=0` · `usable_llm_text`(`.` 등 junk=empty) · empty 시 raw meta 로그 · [`secure-rag.md`](../references/secure-rag.md) 주석
- LLM 요약 재개 시: 채팅용 더 큰 모델 + `SECURE_GENERATE=1`

## 채팅 복원 + Documents 변환/감시 (v1)

- API: `GET /chat/threads` · `GET /chat/threads/{id}/messages` (channel=general|security) + Express 패스스루
- FE: SecurityChatbot · GlobalChatbot 스레드 목록 / 복원 / 새 대화
- **듀얼 엔진:** PDF/TXT → `Documents/ai-service/*.md` · CSV/XLSX → Move `data/csv_lake/` + `*-profile.md` (메타만)
- Watch: debounce 4s + ingest coalesce lock ([`document_watcher.py`](../../ai-service/agent/document_watcher.py)) · FastAPI 비블로킹
- 의존성: `openpyxl`, `watchdog` — 설치 완료
- 이미지 OCR · `SECURE_GENERATE` / `fillThreshold` / ingest 청크 로직 변경 없음
