# 2026-07-22 프로젝트 작업 기록

## 변경 요약

마이그레이션(07-21) 이후, **문서·AI 규칙 구조를 모노레포 기준으로 정리**하고 README / AGENTS 역할을 나눴습니다.  
이어서 Vite 원본의 **Issue / Knowledge / Inquiry / Dashboard** 페이지 UI를 Next.js로 이전하고, **공통 AppShell**로 사이드바·헤더 네비를 통합했습니다.  
헤더 유저 아이콘에서 **`/login`으로 이동**할 수 있게 연동했습니다 (Login UI는 이후 작성).  
추가로 **ai-service 1단계 O/X 진단**용 프롬프트를 보강·스키마 고정했습니다 (학습 코드는 미구현).

---

## 1. frontend README 정리

- `frontend/README.md`를 원본 Vite README 양식에 맞춰 Next.js 기준으로 재작성했습니다.
- `react-dom` 등 프레임워크 내부 의존성은 Dependencies에 넣지 않고, 앱에서 쓰는 라이브러리만 표기했습니다.
- **개발 기록은 README에 상세를 쓰지 않고**, `docs/work-log/` 링크만 남기도록 정리했습니다.

---

## 2. docs를 모노레포 루트로 이전

작업 방향·일지·계획·프롬프트·참조를 **프로젝트 전체**가 쓰도록 루트 `docs/`로 모았습니다.

| 경로 | 용도 |
|------|------|
| `docs/direction.md` | 현재 작업 방향 (항상 최신 1개) |
| `docs/work-log/` | 날짜별 상세 · 일일업무보고용 |
| `docs/plans/` | 확정 계획 요약 |
| `docs/prompts/` | 재사용 프롬프트 |
| `docs/references/` | 중요 경로·참조 |
| `docs/README.md` | docs 목차 |

- `frontend/docs/README.md`는 **안내(포인터)만** 두고, 본문 기록은 루트 `docs/`를 보도록 했습니다.
- `docs/plans/2026-07-21-nextjs-migration.md`에 마이그레이션 계획 요약을 남겼습니다.

---

## 3. AI 룰·스킬 구조 (전체 vs 개별)

이전 워크스페이스(`Downloads/KDT-Project`)에만 있던 룰·스킬을 현재 모노레포(`.cursor/`, `.agents/`)로 맞추고, 범위를 정리했습니다.

| 구분 | 의미 | 예 |
|------|------|-----|
| **전체 룰** | 저장소 어디를 건드려도 적용 | `main-project.mdc`, `docs-workflow.mdc` |
| **전체 스킬** | 작업을 어디에 맡길지 조율 | `project-control` |
| **개별 룰·스킬** | 특정 중요 화면·API만 | Management / Setting, `*-api` |

주요 보완:

- 문서·일지 범위가 `frontend/docs`에 묶여 있던 부분을 **루트 `docs/`** 기준으로 수정
- 페이지·API globs를 `frontend/src/...` 경로에 맞게 조정
- 중요 페이지용 개별 룰(예: management / setting) 골격 추가

---

## 4. README / AGENTS 역할 분리

모르는 사람이 읽어도 “어디에 무엇이 있는지” 보이도록 네 파일을 역할별로 다듬었습니다.

| 파일 | 누구용 | 담는 내용 |
|------|--------|-----------|
| 루트 `README.md` | 사람 | 저장소 지도, 실행 입구, docs·룰이 어떻게 나뉘는지 |
| 루트 `AGENTS.md` | AI | 전 패키지 공통 **짧은** 규칙 |
| `frontend/README.md` | 사람 | FE 실행·스택·구조·진행 (전체 룰 설명은 루트로) |
| `frontend/AGENTS.md` | AI | 루트 AGENTS 안내 + FE **추가** 규칙만 |

한 줄 원칙:

- **README** = 설명서 (사람)
- **AGENTS** = 수칙 규칙 (AI, 짧게)
- **docs/** = 방향·일지 (상세)

---

## 5. Issue / Knowledge / Inquiry 페이지 마이그레이션

원본: `Downloads/KDT-Project/src/pages/{Issue,Knowledge,Inquiry}Page.tsx`  
대상: `frontend/src/app/{issue,knowledge,inquiry}/page.tsx`

| URL | 변환 |
|-----|------|
| `/issue` | `'use client'` + `export default function IssuePage` |
| `/knowledge` | `'use client'` + `export default function KnowledgePage` |
| `/inquiry` | `'use client'` + `export default function InquiryPage` (lucide-react) |

- react-router 의존 없음 → `useRouter` 치환 불필요
- `npm run build` 성공, `/issue` · `/knowledge` · `/inquiry` 정적 생성 확인

---

## 6. Dashboard 마이그레이션 + 공통 AppShell

- Dashboard: Vite `DashBoardPage.tsx` → `frontend/src/app/(shell)/dashboard/page.tsx`
- Main: `/` → `/main` (`(shell)/main/page.tsx`), 루트 `page.tsx`는 `redirect('/main')`
- 공통 UI: `src/components/layout/AppShell.tsx` + `(shell)/layout.tsx`
  - 좌측 메뉴: Main / Dashboard / Issue / Knowledge / Inquiry / Management / Setting (`next/link`)
  - 상단 헤더 공유
  - Login(`/login`)만 셸 밖
- Main / Management / Setting에서 중복 사이드바·헤더 제거
- `npm run build` 성공 (`/main`, `/dashboard` 포함)

---

## 7. 페이지 연동 · Login 진입점

셸 적용 페이지끼리 좌측 메뉴로 이동 가능하도록 정리했고, Login은 셸 밖이지만 **어디서든 들어갈 수 있게** 헤더에 연결했습니다.

| 구분 | 연동 |
|------|------|
| `/` | `/main`으로 리다이렉트 |
| 좌측 메뉴 | Main · Dashboard · Issue · Knowledge · Inquiry · Management · Setting (`next/link`) |
| 헤더 유저 아이콘 | `/login`으로 이동 (UI는 placeholder, 라우트·진입만 준비) |
| `/login` | AppShell 없음 (독립 페이지) |

---

---

## 8. ai-service 1단계 O/X 프롬프트 보강

원본 학습 프롬프트 리뷰에서 빠진 계약·절차를 문서에 고정했습니다. **코드(`train_pipeline.py`)는 아직 미구현**입니다.

| 경로 | 내용 |
|------|------|
| `docs/references/cathode-clf-schema.md` | CSV 경로·고정 컬럼·타깃 0/1·검증 규칙 |
| `docs/prompts/train-pipeline-ox-classifier.md` | 보강 프롬프트 (refit, predict 단건, 전역 Top-3, Optuna space, GPU fallback, JSON 스키마) |
| `docs/plans/2026-07-22-ox-classifier-prompt.md` | 확정 결정 요약 |

확정 요약: `top_risk_factors`=전역 SHAP Top-3, Optuna 후 Train 전체 refit·Test 1회, 앙상블 가중치 고정, LangGraph/LLM/제어는 범위 밖.

`ai-service/AGENTS.md` — 챗봇·AI 모델 생성 시에만 보는 패키지 내 1차 참고서 (원본 프롬프트 + 보강 확정 + 스택·predict 계약).

---

## 9. ai-service 1단계 train_pipeline 구현

| 산출물 | 설명 |
|--------|------|
| `ai-service/train_pipeline.py` | `train_model()` / `predict(df, fillThreshold=0.5)` |
| `ai-service/requirements.txt` | polars, xgboost, catboost, optuna, shap 등 |
| `ai-service/.gitignore` | `models/*`, `optuna.db` 등 |
| `ai-service/models/` | 스모크 학습 아티팩트 (git 제외) |

검증: `OPTUNA_TRIALS=2`, `USE_GPU=0`으로 end-to-end 학습 성공.  
Test ROC-AUC ≈ 0.910, `predict` 단건 OK. 전역 Top-3: metal_impurity, humidity, sintering_temp.  
정식 100 trial은 `optuna.db` 삭제 후 기본값으로 재실행하면 된다.

### 스모크 실행 시각 (2026-07-22 KST, 사전 승인 없이 에이전트 실행)

| 작업 | 시각 | 내용 |
|------|------|------|
| 의존성 설치 | ~17:11 | `pip install -r requirements.txt` |
| 학습(스모크) | **17:12:06 ~ 17:13:30** | `OPTUNA_TRIALS=2`, `USE_GPU=0`, `python train_pipeline.py` |
| predict 테스트 | **17:13:45** | CSV 1행 `predict(...)` |

이후 정책: 설치·학습·테스트는 실행 전 사용자 승인 필수 → [`.cursor/rules/ask-before-run.mdc`](../../.cursor/rules/ask-before-run.mdc) (2026-07-23 추가).

---

## 현재 상태와 다음 작업

- frontend UI: Main · Dashboard · Issue · Knowledge · Inquiry · Management · Setting + AppShell
- Login: 라우트·헤더 진입 연동 완료, UI는 placeholder
- ai-service: `train_pipeline.py` 구현·스모크 학습 완료 (`models/` 아티팩트 로컬 생성)
- **다음:** Login UI, backend API, (선택) Optuna 100 trial 정식 재학습, FastAPI `predict` 노출
