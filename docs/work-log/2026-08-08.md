# 작업 일지 — 2026-08-08

## 요약

- 메인 「위험 LOT Top」: `analysis_lots` 연동 · 컬럼 UI · 최근 3일·SPC 이탈·심각 · 8건 페이지네이션
- 메인 당일 KPI: `probability` 기준 양품/불량 · AI O/X 임계값 **0.8**
- 대시보드: LOT 위험등급 탭(8행·전체보기·CSV/PDF) + 「생산 상세」탭 스위치
- 생산 상세: CSV 목업 제거 → `lots`+`analysis_lots` 실데이터 · 최근 7일 · 공정 AVG · operator/d50/d90 필터
- 병합 후속: 대시보드 JSX·`lotScore.ts` 충돌 정리

---

## 메인 · 위험 LOT Top

**상태:** `GET /api/lots/risk-top` + Main UI 연동 완료.

| 항목 | 내용 |
|------|------|
| 표시 | LOT=`lot_id` · 위험 원인=`risk_reason` · 「상세」→「상세보기」 · 위험등급 열 →「챗봇으로 진단」 |
| 필터 | `lots.timestamp` 최근 **3일** · `spc_status LIKE '%이탈%'` · `risk_level = '심각'` |
| 페이지 | **8건**/페이지 · `total`/`totalPages` · 이전/번호/다음 |
| FE | [`main/page.tsx`](../../frontend/src/app/(shell)/main/page.tsx) · [`mainApi.ts`](../../frontend/src/api/mainApi.ts) |
| BE | [`lot.service.ts`](../../backend/src/services/lot.service.ts) `getRiskTop({ page, pageSize })` |

---

## 메인 · 당일 KPI · 임계값 0.8

**상태:** 당일 `analysis_lots.probability` 집계 · O/X 판정 임계값 0.8.

| 항목 | 내용 |
|------|------|
| 기간 | `lots.timestamp >= CURDATE()` |
| 양품 | `probability < 0.8` · 불량 `>= 0.8` |
| 상수 | `DEFECT_JUDGE_THRESHOLD = 0.8` ([`lotScore.ts`](../../backend/src/services/lotScore.ts)) |
| 모델 | [`ensemble_config.json`](../../ai-service/models/ensemble_config.json) `default_threshold` 0.4→**0.8** |
| API | `GET /api/lots/daily-kpi` · Main KPI 카드 연동 |
| 참고 | 위험등급용 `DEFECT_PROB_SEVERE`(0.4)는 유지 · `"LOT를 찾을 수 없습니다"`는 `getLotById` 404(라우트 순서/재시작 이슈와 구분) |

---

## 대시보드 · LOT 위험등급 탭

**상태:** 상단 카드에 탭 스위치 · 하단 단독 생산 테이블 제거 · Grafana 유지.

| 항목 | 내용 |
|------|------|
| 탭 | **LOT 위험등급** \| **생산 상세** |
| LOT 위험 | 페이지당 **8행** · 높이 `h-[512px]` · 전체보기 모달 · CSV/PDF(BOM·인쇄 HTML) |
| FE | [`dashboard/page.tsx`](../../frontend/src/app/(shell)/dashboard/page.tsx) |

---

## 대시보드 · 생산 상세 실데이터 · 최근 7일

**상태:** `getProductionDaily` 재작성 · FE 컬럼·필터 반영.

### 집계 규칙

| 컬럼 | 소스 |
|------|------|
| 날짜 | `lots.id` `LOT-YYYYMMDD-…` → `YYYY-MM-DD` (실패 시 `timestamp` 폴백) |
| 총생산량 | 당일 LOT **행 수** |
| 양품 / 불량 | `probability < 0.8` / `>= 0.8` · **NULL은 양품·불량 제외, 총생산에는 포함** |
| 불량률 | 불량수 / 총생산량 |
| 금속 불순물·소성 온도·습도·리튬 투입량·첨가제 비율·압력·공정시간 | 당일 LOT **평균** · 컬럼 NULL은 **평균에서 제외**(전부 NULL이면 `null`/`-`) |

- 기간: 최신 lot_id 날짜 기준 **최근 7일** (기본 1페이지·최대 7행)
- 제거: `소성온도×습도`, `temp_dev_from_800`(이탈)
- 이름: 소성온도 이탈 → **소성 온도** (`sintering_temp` AVG)
- 필터: `operatorId`, `d50`/`d90` min·max — 개별 또는 동시 **AND** · 아이콘+입력 UI
- CSV 체크박스 다운로드(`lots.csv?date=`) 유지

### 관련 파일

- BE: [`dashboard.service.ts`](../../backend/src/services/dashboard.service.ts) · [`dashboard.controller.ts`](../../backend/src/controllers/dashboard.controller.ts)
- FE: [`dashboardApi.ts`](../../frontend/src/api/dashboardApi.ts) · [`dashboard/page.tsx`](../../frontend/src/app/(shell)/dashboard/page.tsx)

---

## Git / 병합 정리

- `Mainpage_API` ↔ hotfix 원격 분기·PR 머지 후 대시보드 JSX(테이블 footer·Grafana)·`lotScore.ts` conflict marker 정리
- `DEFECT_JUDGE_THRESHOLD`와 `getStandardDefaults()` 기반 severe/caution 상수 공존 유지

---

## 범위 밖 (오늘 미포함)

- 생산 추이 차트·Grafana 패널 자체 변경
- 스키마 DDL 변경
- LOT 위험등급 탭의 판정 로직 재설계

---

## 이전 스냅샷 링크

- [`2026-08-06`](./2026-08-06.md) — 생산 추이 · model_quality · blending
- [`2026-08-05`](./2026-08-05.md) — judgment.probability · SPC 폴링 · residual
