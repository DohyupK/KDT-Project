# 2026-07-30 업무일지

## 요약

RAG 선행 작업: fixture 6건 · 가드레일 C·D · min_score 0.05 유지.  
이어서 **Self-Query 필터(A) LI 교체** + **API sources E2E 스모크** 스크립트 추가.

## Fixture · ingest

신규 (`ai-service/data/secure_docs/`):

- `manual-mixing-v1.md` — 매뉴얼 / mixing
- `sop-coating-v1.md` — SOP / coating
- `rule-lithium-v1.md` — 규정 / lithium_input
- `manual-metal-v1.md` — 매뉴얼 / metal_impurity

`ingest_secure.py` 재실행 → Qdrant `secure_docs` **points=6**, BM25 nodes=6.  
heuristic에 lithium/금속/투입비 등 process 키 최소 확장.

## 가드레일 계약

[`docs/references/secure-rag.md`](../references/secure-rag.md)에 명시:

- SelfQueryRetriever(실체: `VectorIndexAutoRetriever`)는 unfiltered 재시도를 **내장하지 않음**
- 교체 시에도 `retrieve()` 외곽의 **C(empty+filters→unfiltered 1회)** · **D(min_score)** 유지 필수

## 리랭크 점수표 (llm_invoke=None, min_score=0.05)

| 케이스 | n | top score / process | 판정 |
|--------|---|---------------------|------|
| 소성 SOP | 1 | 0.97 sintering | PASS |
| 습도 매뉴얼 | 1 | 0.998 humidity | PASS |
| 혼합 매뉴얼 | 1 | 0.999 mixing | PASS |
| 코팅 SOP | 1 | 1.000 coating | PASS |
| 리튬 규정 | 1 | 1.000 lithium_input | PASS |
| 금속 매뉴얼 | 1 | 0.998 metal_impurity | PASS |
| 코팅 교차 | 1 | 0.997 coating | PASS |
| 빈 category → unfiltered 재시도 | 2 | 0.97 sintering | PASS (가드레일 C) |
| 잘못된 nonempty category(규정)+소성질의 | 0 | — | PASS (min_score가 오매칭 차단) |
| 점심 메뉴 / 날씨 | 0 | — | PASS |

**결론: `SECURE_RERANK_MIN_SCORE=0.05` 유지.**

## Self-Query (A) 교체

- `parse_metadata_filters` → LlamaIndex `VectorIndexAutoRetriever.generate_retrieval_spec` (필터만)
- LLM: vLLM OpenAI 호환 (`CHAT_VLLM_BASE_URL`) · 클라우드 폴백 없음
- `llm_invoke=None` 또는 Self-Query 실패 → `_heuristic_filters`
- `retrieve()` B→C→D **변경 없음**
- deps: `llama-index-llms-openai`, `llama-index-vector-stores-qdrant` (+ README 스택)

## API E2E 스모크

스크립트: `ai-service/scripts/smoke_secure_rag_e2e.py`

| 케이스 | 기대 |
|--------|------|
| 소성 SOP | `mode=security_rag`, `sources`≥1, `text`/`title` 비어 있지 않음, reply `[출처:` |
| 점심 메뉴 | `mode=security_no_docs`, `sources=[]` |

실행 결과 (2026-07-30):

| 단계 | 결과 |
|------|------|
| Qdrant + retrieve(heuristic) | PASS — SOP 1 hit (score≈0.998), lunch 0 hit |
| `POST /security-chat` 전체 | **FAIL (exit 3)** — vLLM `:8001` 미기동 · 패키지 `vllm` 미설치 · 가짜 PASS 없음 |
| Express `:3001` | 미실행(옵션) |

vLLM 기동 후 재실행:

```bash
cd ai-service
python scripts/smoke_secure_rag_e2e.py
# optional: SMOKE_BACKEND_URL=http://127.0.0.1:3001 python scripts/smoke_secure_rag_e2e.py
```

## 수동 Maximize 체크리스트 (브라우저)

데이터 경로는 API `sources[]`와 동일 — Playwright 없음.

- [ ] GlobalChatbot Maximize → SecurityChatbot fullscreen
- [ ] SOP 질의 → reply에 `[출처: …]` 링크
- [ ] 출처 클릭 → 청크 패널 텍스트 = `sources[].text`

## 다음

1. vLLM `:8001` 기동 → API 스모크 PASS · Maximize 수동 체크
2. TS 불량률 · Login/MariaDB 홀딩
